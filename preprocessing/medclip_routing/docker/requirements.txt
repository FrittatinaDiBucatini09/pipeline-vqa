# ==============================================================================
# PROJECT DEPENDENCIES: MedCLIP Agentic Routing (NLP Middleware)
# ==============================================================================
# Optimized for a headless Linux server environment (UniboNLP Cluster).
# PyTorch is installed separately in the Dockerfile with explicit CUDA support.
# ==============================================================================

# --- Core Data & System Utilities ---
numpy>=1.21.0,<2.0.0
pandas>=1.3.0
tqdm>=4.62.0

# --- Deep Learning Framework (PyTorch) ---
torch>=2.0.0

# --- Gemma-2-2B-it (via HuggingFace Transformers) ---
transformers>=4.30.0
huggingface-hub>=0.16.0
accelerate>=0.20.0

# --- SciSpacy (NLP Entity Extraction) ---
spacy>=3.5.0
scispacy>=0.5.0

# --- Experiment Tracking ---
wandb>=0.16.0
