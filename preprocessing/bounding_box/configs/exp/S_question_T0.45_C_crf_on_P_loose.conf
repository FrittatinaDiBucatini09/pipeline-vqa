# GRID SEARCH CONFIG: S_question_T0.30_C_crf_on_P_smart.conf
# Strategy: S_question
# Threshold: 0.30
# Refinement: C_crf_on
# Padding: P_smart
# SECOND BEST
#
# NOTE: This config is designed for stratified 4K sample (mimic_ext_sample_4k.csv)
# which preserves the full dataset's question type and anatomical region distribution.

USE_VISUAL_REGIONS="false"
USE_DYNAMIC_PROMPTS="true"
INCLUDE_CONTEXT_IN_INFERENCE="false"
CAM_THRESHOLD=0.30
SKIP_CRF="false"
ENABLE_SMART_PADDING="true"
OUTPUT_FORMAT="jsonl"

# Use stratified sample instead of STOP_AFTER to avoid bias
METADATA_FILENAME="mimic_ext_sample_4k.csv"