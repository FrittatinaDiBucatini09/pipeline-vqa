# ==============================================================================
# EXPERIMENT 4: HYBRID CONTEXT (Multimodal Prompt Fusion)
# ==============================================================================
# Configuration: Fuses anatomical priors with clinical questions for contextualized reasoning.
# Purpose: Evaluate synergy between structured anatomical knowledge and unstructured clinical queries.
# ==============================================================================
MODE="inference"
USE_DYNAMIC_PROMPTS="true"               # Incorporate clinical question
USE_VISUAL_REGIONS="true"                # Enable anatomical region guidance
COMPOSITE_REGIONS="true"                 # Fuse regions into unified representation
INCLUDE_CONTEXT_IN_INFERENCE="true"      # Inject anatomical context into prompt sequence
CAM_THRESHOLD=0.40
ENABLE_SMART_PADDING="true"              # Maintain anatomical boundary constraints