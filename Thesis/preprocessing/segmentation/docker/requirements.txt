# ==============================================================================
# UNIFIED REQUIREMENTS: Localization + Segmentation Pipeline
# ==============================================================================

# --- Core Data & System ---
numpy>=1.21.0
pandas>=1.3.0
tqdm>=4.62.0

# --- Computer Vision ---
# Headless per server environments (no X11)
opencv-python-headless>=4.5.0
Pillow>=9.0.0
scikit-image>=0.19.0
matplotlib>=3.7.0

# --- Deep Learning (PyTorch) ---
# Installato esplicitamente nel Dockerfile, ma listato qui per completezza
# torch>=2.0.0
# torchvision>=0.15.0

# --- STEP 1: Localization (BiomedCLIP + CAM) ---
open_clip_torch>=2.20.0
transformers>=4.30.0
huggingface_hub>=0.16.0
regex
ftfy
grad-cam>=1.4.8
ttach
wandb>=0.16.0

# PyDenseCRF (Build from source per compatibilità GCC)
git+https://github.com/lucasb-eyer/pydensecrf.git

# --- STEP 2: Segmentation (MedSAM) ---
# Segment Anything Model (Facebook Research)
git+https://github.com/facebookresearch/segment-anything.git